/**
 * 함수 선언하기
 * 1) function 예약어 사용
 * 2) 화살표 기호 사용
 * 
 * 익명함수 : 함수에는 기본적으로 이름이 없으므로 다른 곳에서 사용할 수 없다. 이런 이름이 없는 함수를 익명함수라고 한다.
 * 
 * function() {};
 * () => {}
 */

/**
 * 이름을 붙이면 다른 곳에서 사용할 수 있다.
 * function a() {}
 * const b = function() {};
 * const c = () => {};
 */

function a() {};

a();